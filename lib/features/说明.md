### features/ — 业务功能模块
**分类依据**：按业务功能模块化，每个 feature 内部独立。

#### Feature 内部结构规范
| 子目录 | 用途 | 是否必需 |
|--------|------|---------|
| `pages/` | 页面 Widget | ✅ 必需 |
| `controller/` | 页面状态管理（StateNotifier） | ✅ 必需（有状态时） |
| `widgets/` | 页面专用组件 | ❌ 可选（只有多组件时） |